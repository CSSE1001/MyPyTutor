SHORT_DESCRIPTION = 'Write a function mean(xs), which returns the mean (average) of xs'
HINTS = [
    'The built-in functions <tt>sum</tt> and <tt>len</tt> may be useful',
    'Make sure you use float division (/), not integer division (//)',
]