SHORT_DESCRIPTION = 'Write the function validate_input(string) in the prescribed format'
STUDENT_FUNCTION = 'validate_input'
HINTS = [
    'You will probably find str.split to be handy',
    'You should use a try/except statement to check if you can convert the arguments to floats',
]