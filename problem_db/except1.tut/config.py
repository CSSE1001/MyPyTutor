SHORT_DESCRIPTION = 'Write a function try_int(string) which coverts string into an integer if possible, and returns None otherwise'
STUDENT_FUNCTION = 'try_int'
HINTS = [
    'You will need to catch a ValueError',
]