SHORT_DESCRIPTION = 'Define a function get_digits(str1) which returns all of the digits in str1 as a string'
HINTS = [
    'There is a string method that tests if a character is a digit',
    'Use dir(str), and look for methods starting with \'is\'',
]